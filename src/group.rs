//! CORE: tier-agnostic signature bucketing + leave-one-out over a canonical
//! matrix → [`CandidateGroup`]s.

/// A reported duplicate-candidate group. Assembled in Phase 4.
pub struct CandidateGroup;
